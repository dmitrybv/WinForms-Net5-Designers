# WinForms-Net5-Designers

Project and the ControlLibrary the Visual Studio Form designer doesn’t use ControlDesigners from the ControlLibrary, even though I correctly bind designers to the component.
